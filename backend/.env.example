# Application Settings
APP_ENV=development
SERVER_PORT=8080

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=court_reservation_db
DB_SSLMODE=disable

# Security & Authentication
JWT_SECRET=secret-key
JWT_EXPIRATION_HOURS=24